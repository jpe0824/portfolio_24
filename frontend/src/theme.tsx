import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#fff200",
    },
    secondary: {
      main: "#7b00c8",
    },
  },
});

export default theme;
